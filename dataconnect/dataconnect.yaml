specVersion: "v1beta"
serviceId: "sourcesearch"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "sourcesearch-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
